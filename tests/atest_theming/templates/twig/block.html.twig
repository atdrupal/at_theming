{#

  Render me:

    $output = at_container('twig')->render('@atest_theming/templates/twig/block.html.twig');

#}

Block content only:

{{ 'system:powered-by' | drupalBlock(1) }}

Block with full template

{{ 'boxes:demo-drupalbox' | drupalBlock }}
